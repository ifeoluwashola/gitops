apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo
  namespace: demo
spec:
  selector:
    matchLabels:
      app: demo
  endpoints:
    - port: http
      path: /metrics
